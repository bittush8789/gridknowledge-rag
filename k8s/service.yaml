apiVersion: v1
kind: Service
metadata:
  name: gridknowledge-service
  namespace: gridknowledge
  labels:
    app.kubernetes.io/name: gridknowledge-rag
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: gridknowledge-rag
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30080
